{
  "name": "representative-v2",
  "visibility": "public",
  "description": "Exercise representative Codex-to-ChatGPT Web adaptations.",
  "dependencies": [
    "fixture-dependency"
  ],
  "sourceProvenance": {
    "type": "absent"
  },
  "projection": {
    "entrypoint": "source.md",
    "sources": [
      {
        "path": "source.md"
      },
      {
        "path": "helper.md"
      }
    ],
    "changeRecords": [
      {
        "allowedRuntimeChange": "equivalent-mechanism",
        "source": "source.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "chatgpt-sandbox"
          ],
          "incompatibility": "The Codex fixture writes ephemeral scratch state to a local path; ChatGPT Web provides sandbox working state instead."
        },
        "transform": {
          "type": "replace-exact",
          "match": "Write scratch state under `/tmp/fixture-work`.",
          "replacement": "Write scratch state in the ChatGPT sandbox."
        }
      },
      {
        "allowedRuntimeChange": "translate-invocation-or-tool",
        "source": "source.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "connected-github"
          ],
          "incompatibility": "The fixture's local Git CLI mutation must become the same repository mutation through connected GitHub."
        },
        "transform": {
          "type": "replace-exact",
          "match": "Run `git commit -am \"record result\"` after updating the repository.",
          "replacement": "Commit the same repository changes through connected GitHub and observe the returned commit result."
        }
      },
      {
        "allowedRuntimeChange": "translate-invocation-or-tool",
        "source": "source.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "chrome-browser-mcp"
          ],
          "incompatibility": "The workflow depends on the user's already-open browser state rather than a dedicated automation context."
        },
        "transform": {
          "type": "replace-exact",
          "match": "Continue the workflow in the user's already-open Chrome tab using local Chrome automation.",
          "replacement": "Continue the workflow in the user's existing Chrome session through Chrome Browser MCP."
        }
      },
      {
        "allowedRuntimeChange": "translate-invocation-or-tool",
        "source": "source.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "chatgpt-child-workers"
          ],
          "incompatibility": "The upstream parallel subagents require genuine context isolation and parallel execution, not sequential simulation."
        },
        "transform": {
          "type": "replace-exact",
          "match": "Dispatch two isolated subagents in parallel. If real isolation or parallel dispatch is unavailable, stop this operation.",
          "replacement": "Dispatch genuinely independent ChatGPT child workers in parallel only after Live Capability verifies the required isolation and parallelism; otherwise stop this operation."
        }
      },
      {
        "allowedRuntimeChange": "translate-invocation-or-tool",
        "source": "source.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "connected-github"
          ],
          "incompatibility": "The Repository Asset must remain durable and versioned without assuming host filesystem persistence."
        },
        "transform": {
          "type": "replace-exact",
          "match": "It is versioned beside this skill.",
          "replacement": "Keep it versioned beside this skill in connected GitHub."
        }
      },
      {
        "allowedRuntimeChange": "select-upstream-supported-branch",
        "source": "source.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "no-native-application-control"
          ],
          "incompatibility": "GitHub can store the Repository Asset but the target does not provide an assumed Keynote consumer."
        },
        "transform": {
          "type": "replace-exact",
          "match": "If Keynote is unavailable, stop only the export operation.",
          "replacement": "Because GitHub storage does not prove a live consumer can use the Repository Asset, stop only the export operation unless a Live Capability for consuming it is verified."
        }
      },
      {
        "allowedRuntimeChange": "select-upstream-supported-branch",
        "source": "source.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "no-native-application-control"
          ],
          "incompatibility": "The target does not assume host macOS native application control for osascript."
        },
        "transform": {
          "type": "replace-exact",
          "match": "On macOS, send a native notification with `osascript`. If `osascript` is unavailable, stop only the notification operation.",
          "replacement": "The target has no assumed native macOS application control and no Equivalent Mechanism for this notification, so select the upstream-supported fallback and stop only the notification operation."
        }
      },
      {
        "allowedRuntimeChange": "inline-supporting-document",
        "source": "source.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "only-load-skill-and-list-skills"
          ],
          "incompatibility": "The required helper is inlined into the Generated Runtime, so the entrypoint must retarget its Supporting Document reference to the inlined material."
        },
        "transform": {
          "type": "replace-exact",
          "match": "Follow `helper.md` to normalize labels before scoring.",
          "replacement": "Use the inlined Deterministic Helper below to normalize labels before scoring."
        }
      },
      {
        "allowedRuntimeChange": "inline-supporting-document",
        "source": "helper.md",
        "evidence": {
          "targetRuntimeProfile": "chatgpt-web-mcp-v2",
          "constraints": [
            "only-load-skill-and-list-skills"
          ],
          "incompatibility": "The required deterministic helper must be available through the loaded Generated Runtime while preserving its exact algorithm."
        },
        "transform": {
          "type": "append-source",
          "separator": "\n\n## Inlined Supporting Document\n\n"
        }
      }
    ]
  }
}
